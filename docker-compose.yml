version: "3.1"
services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    network_mode:  host
  app:
    build:
      context: .
    depends_on:
      - redis
    ports:
      - "3000:3000"
    network_mode: host
    